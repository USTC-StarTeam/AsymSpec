# Copy to .env and fill in your own credentials. Never commit .env.
# Tavily search API key — used by the GAIA web-agent harness
# (scripts/asym_smolagents/) when the DDG cache misses.
TAVILY_API_KEY=
# Hugging Face token — needed for gated datasets (gaia-benchmark/GAIA).
HF_TOKEN=

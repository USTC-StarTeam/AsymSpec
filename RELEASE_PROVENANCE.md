# Release provenance

This public-release candidate was exported from the authors' collaborative
private research repository at source commit:

```text
b8eacc3159b8a116fb3fff6cdd4eaa5096b2e82f
```

The source snapshot was authored and maintained by the project collaborators.
For compliance and security, this release uses a new Git history under the
`LesterLv` GitHub account rather than mirroring private branches and deleted
files. The source commit is recorded here to preserve provenance; full paper
authorship and affiliations are recorded in `AUTHORS.md` and `CITATION.cff`.

The export excludes private Git history, internal notes, raw experimental
outputs, cached web/tool responses, datasets, model weights, credentials, and
machine-specific environment files.
